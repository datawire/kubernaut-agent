# file: bootstrap.vars
#
# Contains KEY=VALUE pairs that can be sourced from the bootstrap shim. The keys and values need to be environment
# variable friendly.
#

BOOTSTRAP_REPO="${bootstrap_repo}"
BOOTSTRAP_BUNDLE="${bootstrap_bundle}"
